// import { useReducer } from "react";
// type Action = { type: "add"; payload: Function } | { type: "remove"; payload?: undefined };

// type State = Function[];

// const reducer = (state: State, { type, payload }: Action): State => {
//   switch (type) {
//     case "add":
//       return state + 1;
//     case
//   }
// }
// function CheckedContents() {
//   const [checkedArr, dispatch] = useReducer((state, action) => {
//     switch(action.type) {
//       case "add":
//         return state.push(...checkedArr)
//     }
//   })
// }
